{
  "name": "@nznt/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/helmet": "^13.0.2",
    "@fastify/rate-limit": "^10.3.0",
    "@nznt/auth": "workspace:*",
    "@nznt/contracts": "workspace:*",
    "@nznt/db": "workspace:*",
    "@nznt/script-protocol": "workspace:*",
    "fastify": "^5.6.2",
    "nodemailer": "^7.0.10",
    "otplib": "^12.0.1",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@types/nodemailer": "^7.0.4",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
